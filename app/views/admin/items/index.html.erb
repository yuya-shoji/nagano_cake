<h1>投稿一覧</h1>
<p>タイトル</p>
<% @items.each do |item| %>

  <p><%= item.name %></p>
  <span>
    <%= link_to item.name, admin_item_path(item.id) %>
  </span>
  <p>画像</p>
  <%= attachment_image_tag item, :image, :fill, 100, 100, format: 'jpeg' %>
<% end %>
